# PersistentUAVThroughput-v1

Read README.md and MINIMAL_PERSISTENT_THROUGHPUT_SPEC.md first.
This is a separately scoped project, not a continuation of the closed regenerative-control experiments.
The legacy source at /home/zjl/mappo is a read-only dependency.
Authorized: fix empty-queue recharge/idle, complete the current physical calibration,
navigation qualification report and independent reproducibility evidence.
B0–B5 implementation exists, but running baseline experiments is NOT currently authorized.
No neural training, MPC, counterfactual safety branching, or small oracle without new authorization.
Preserve the user's collision recovery and LiDAR-only deployment information contract.
After focused correctness checks, a tiny execution smoke and first resumable checkpoint health check,
hand the run back by default. The latest user explicitly authorized completion of
the current 1000-job calibration; do not automatically launch the next phase.
Current pilot size is fixed at 1000 before outcomes; do not select maps or parameters by baseline gap.
