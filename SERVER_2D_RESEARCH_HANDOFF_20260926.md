# 二维双约束研究交接：已完成结果与下一实验

日期：2026-09-26。交接分支：`research/uav-learning-20260926`。
服务器侧由独立 Codex 管理检出、ARM64 适配、作业启动与结果回传；本机不再启动二维训练。
本文件记录可核对的起点和**建议的下一实验**，不把尚未运行的方案写成结果。

## 当前研究问题

在合成二维、静态地图、单无人机持续单目标任务中，学习策略每个高层决策点
选择局部航向/速度、主动返站或在站补能。低层 CBF/QP 避碰器与可执行返站
备用路径构成评价安全层。研究问题是：**当避碰修正改变航迹与耗能时，
策略能否学会自己决定继续任务、主动返站与补能时机，同时保持可返站性与
固定时窗内的任务吞吐量？** 需区分策略本身与安全层代替策略作决定。

这不是完整三维配送：没有取货→送达、订单队列、超时承诺或真实 M100
功率曲线。三维导航与 1 km 能耗组件在本分支中另行保留，不能将其资格结果
合并为二维学习结论。

## 已冻结的起点

- 完整报告：[`MODE_PPO_V4_LAMBDA1_RESULT_20260926.md`](MODE_PPO_V4_LAMBDA1_RESULT_20260926.md)。
- v4 seed 101：训练 51,200 高层步；8 张未参与训练的验证图 32–39 共完成
  19 个目标。v3 为 1，解析路线 full 为 61，GPU-MPC H4 为 68；这些只是
  单种子、同图诊断，不能据此声称显著性。
- v4 在共同安全层下 0 碰撞、0 耗尽，但 54 次充电均紧随 54 次强制返站
  接管；策略主动返站动作 9 共 0 次。探索性无安全层重放为 8/8 图耗尽。
  因此“安全层组合可以运行”与“策略学会返站”是不同主张。
- 最终模型：`evidence/mode_ppo_v4_lambda1_seed101_20260926/training/model_000051200.zip`，
  SHA-256 `71ca7c3382f4fb994f90a1334b70fcece5ea5fdf1cb98b3319e069b696858429`。
  训练 manifest、状态、原始验证事件与回放见同目录；
  `EVIDENCE_MANIFEST.json` 记录 38 个证据文件的哈希。
- 64 条政策无关电池标定原始结果及汇总位于
  `artifacts/dual_constraint_2d_calibration_20260926/`，现已纳入分支。
  `calibration.json` SHA-256 为
  `4ca665dc8668b790df43ad718e64f31d8f4ce8b91cea47e288d294d91ab50591`；
  电池容量 85.39121788182787 合成能量单位，满充 29.9 仿真秒。

## 下一实验：安全接管反馈的配对微调

这是**待实现、待启动**的诊断。目标是区分训练/评价安全层不一致与缺少
安全接管学习信号这两个解释，不是预设学习方法必胜。

1. 两组均从同一份 v4 最终**策略权重**初始化，使用新的优化器状态；
   训练地图 8–31、seed 101、`ModeMaskedPolicy`、γ=1、GAE λ=1、
   600 仿真秒 terminal、PPO 其余超参数和每组 25,600 高层步一致。
   每 512 步原子保存模型、状态和 episode 记录；中断后可恢复。
2. 两组训练都启用与评价相同的避碰及可执行返站安全层。**对照组**
   保留已有奖励：每次强制返站接管扣 0.2。**反馈组**对每次强制
   返站再扣 1.8，使其总扣分为 2.0；被安全层拒绝的离站请求扣 2.0。
   普通 QP 避碰修正不新增扣分。两组都不改变物理能耗、碰撞修复、
   能量备用路径或评价安全层；不通过新增电量阈值制造返站行为。
3. 训练结果只用固定最终检查点评价图 32–39。每组同时跑相同地图的
   shielded 与无安全层重放，逐图报告完成目标数、主动返站动作 9、
   强制返站接管、补能、碰撞、耗尽、拒绝离站和安全层干预。无安全层
   重放是策略诊断，不是可部署模式。
4. 首轮只用 seed 101 诊断。若反馈组增加主动返站并减少强制接管与
   无安全层耗尽，同时没有使完成数严重坍塌，再决定是否做多 seed。
   若两组都只靠安全层、或主动返站变成无任务推进的空转，记录失败，
   不按验证差距改动奖励或选中途检查点。

建议服务器侧先复核 ARM64 的 Python、NumPy、SciPy、OSQP、Gymnasium、
PyTorch 与 stable-baselines3 兼容性。当前 `pyproject.toml` 只列出解析导航
基础依赖，不能把 `uv sync --frozen` 当作 PPO 完整依赖安装。
PPO 可在 CPU 上运行；本分支使用 CUDA 的训练参数不自动兼容 Ascend NPU。
服务器资源快照见 `ASCEND_SERVER_INVENTORY_20260926.md`，其 NPU 当时被
vLLM 占用；开跑前由服务器侧重新检查。

保持原有碰撞语义：接触不结束任务，修复位置并清零速度；统一碰撞计数
每个策略步最多一次；原始边界/障碍接触惩罚和连续接触固定折扣不因本
实验修改。只进行必要的改动代码测试、一次新执行路径 smoke 和首检查点
健康检查；健康后由服务器侧接管，不自动推进长训练或新矩阵。
