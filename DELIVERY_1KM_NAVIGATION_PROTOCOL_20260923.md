# 1 km 新尺度导航资格协议

日期：2026-09-23。**运行前固定**：用户已选择分散随机取送点、地面和可达屋顶服务位、每图一座位置随机充电站，并接受以标注假设做部分充电敏感性测试。本协议只检验共享解析导航在新尺度下能否执行航段；此时尚无飞行 Wh 模型或动态订单评价。

使用 [`delivery_1km/qualify.py`](delivery_1km/qualify.py) 的 12 张未用于 60 m 冻结资格的新地图（ID 100–111），每张地图 24 根高度 15–75 m 的有限圆柱、独立随机充电站。每图固定四类航段各 2 条：站→地面、站→屋顶、地面→屋顶、屋顶→站，共 **96 条**。服务点的产生与所评估的导航策略无关；生成时用静态地图筛掉几何上无路的点，执行中导航失败仍原样记录。低层继续使用冻结 v2 控制器，不更新权重或改变接触修复。

统一上限为 800 模拟秒（`0.05 s × 16000` 步），到达半径 5 m。每条记录到达、无路或超时，统一接触数，QP 不可行步、干预步、路线与实际长度、飞行时间，以及路线规划和控制/QP 墙钟时间。终点速度强制置零的人工跳变不纳入未来能耗加速度统计。一个站点/地图只在本地图内随机，算法之间相同。

通过导航资格的预设标准：96 条中至少 92 条到达（≥95%）且所有航段统一接触总数为 0；同时单列地面和屋顶航段失败类型。即使达到标准，也只支持后续调度**诊断**，不意味着真实四旋翼能耗或安全已验证。首次启动仅到两条航段检查点，确认结果及轨迹可读后交还用户，不自动完成剩余 94 条。

一行启动命令：`cd /home/zjl/uav_learning_research && .venv/bin/python -m delivery_1km.qualify --output artifacts/delivery_1km_nav_20260923 --stop-after-checkpoint`

一行续跑命令：`cd /home/zjl/uav_learning_research && .venv/bin/python -m delivery_1km.qualify --output artifacts/delivery_1km_nav_20260923 --resume --stop-after-checkpoint`
