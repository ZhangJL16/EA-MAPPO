# 二维 PPO v3：固定时窗训练定义修复

日期：2026-09-26。v2 seed 101 已完整诊断并冻结，不对原数据补写或覆盖。
本方案仅研究独立二维合成环境；三维服务器实验继续独立进行。

研究问题是：把 600 秒任务终点正确视为 MDP terminal，并消除变时长
动作的折扣混淆后，模式条件 PPO 能否学会在固定窗口内主动补能与
完成目标？答案必须由完成预算后的冻结验证决定，不能根据启动轨迹
或训练图调参数。

## 预设条件

- **主条件**：`gamma=1.0`，600 秒终点 `terminated=True`。这与
  未折扣固定窗口完成数的时间定义一致；训练奖励仍沿用既有目标完成、
  时间、碰撞和耗尽项，不声称奖励与主指标完全相同。
- **分解条件**：`gamma=0.995`，同样正确终止；用于把终止标签改正的
  影响与折扣改正的影响分开。该条件仍按高层决策而非经过的物理时间
  折扣，明确标为诊断，不作为物理时间一致的最终学习方法。
- 两条件均为 seed 101/202/303、每 seed 51,200 高层步；训练图 8–31，
  验证图 32–39，未见确认图预留 56–71。每图评价 600 仿真秒，使用
  同一冻结执行安全层。行动作掩码、物理环境、地图、网络、PPO 其他
  超参数保持 v2 一致。`gamma=1.0` 并不保证 PPO 收敛。
- 对照旧 PPO、v2 模式掩码、`route_full`、GPU-MPC H4/H8。报告每个
  seed/图的完成数、主动充电、站内拒绝、返站接管、碰撞与耗尽，并
  区分原始训练失败和安全层执行后安全。

`FiniteHorizonGym` 只改变 Gym 的终止标签，不改仿真状态、奖励或行动。
本机 SB3 的 `DummyVecEnv` 检查 `TimeLimit.truncated=False`，从而不在
600 秒终点加入 `gamma * V(s_T)`。训练和评价始终可恢复；同参数同
目录续跑，manifest 源码 SHA 防止混用。完整模型训练完成前，评价
入口拒绝其检查点。

主条件首检查点单行命令：

`cd /home/zjl/uav_learning_research && python3 -m learning2d.train_finite_horizon_ppo --output artifacts/mode_ppo_v3_gamma1_20260926/seed101 --seed 101 --gamma 1.0 --total-timesteps 51200 --n-steps 512 --checkpoint-steps 512 --max-new-timesteps 512 --device cuda`

同目录去掉 `--max-new-timesteps 512` 才会续跑剩余长训练。依照实验
启动约束，首检查点健康后交还用户，不自动推进长训练。变时长动作
若以后重新使用 `gamma<1` 作主方法，需实现按实际持续时间计价的
半马尔可夫回报和单独测试。
