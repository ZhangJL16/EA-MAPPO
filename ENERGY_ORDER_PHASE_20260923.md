# 冻结低层后的能耗与订单接口

本阶段使用 [冻结的 v2 导航](NAV3D_V2_FREEZE_20260923.md)，研究高层怎样在当前队列中选单、决定是否去充电站及充到多少。该文件固定**信息边界和实施顺序**；载荷、电池与充电功率的数值尚未冻结，因而不能把现有轨迹换算成真实 Wh 或宣称能量可行。

## 信息流

一次高层决策只读：当前时刻、无人机位置/速度/剩余电量/载荷状态、已知站点、当前可见订单的生成时间/取送点/载荷/deadline，以及低层对候选航段的**飞前**摘要。候选航段接口返回规划是否有路、预计时间、预计能耗或其区间、路线长度/升降量、估计器版本和查询耗时；不可向高层暴露障碍物真值、整张地图或未来订单。低层可以读取事先测绘的静态地图；在线方法共享同一查询接口、缓存规则和安全层。

飞行记录另存每步时间、位置、速度、实际加速度、载荷、电量变化、导航状态、统一接触、CBF/QP 不可行及接管。飞后运动量是物理能耗模型的输入和估计器误差标签，**不能**作为该航段出发前的预测特征。要审计“能否完成取货→送达→安全到站”，应按整条承诺链的能耗区间判定，不能仅检查下一段。

## 订单状态与动作

- 队列容量 10，已取货的订单占一个名额。外生生成的订单在满队列时拒收，仍进入评价分母。未取货的订单可放弃或过期；一旦取货，即使迟到也必须送达。
- 高层每个固定时刻可重评当前队列；飞行中允许改向尚未取货的订单，但不能用反复切换规避已取货履约。每次换目标、航段查询和求解都计入决策时间。
- 补能是到站后的显式动作，可选择目标 SOC 并支付实际充电时间；站内静置不耗飞行电，也不会自动充电。到达截止运营时刻 `T` 后不再取新货，已取货订单继续送达并安全回站。
- 首轮主目标是固定窗口内准时送达数；耗尽、取货后未履约、回站失败和统一碰撞单独报告。未见同类地图按地图 ID 留出；大街区隔断等新结构另成分布外套件。

## 实施顺序与可证伪点

1. **能量计量**：选定带载荷的四旋翼功率模型及公开/实测参数，明确双积分器加速度如何映射为总推力、俯仰和功率；先检查静置、悬停、升降、水平飞行及载荷单调性，再在冻结 v2 轨迹上计算飞后能量标签。未完成这一步前不设凭空的 Wh、SOC 或保底余量。
2. **飞前估计**：只用路线 API 可得量训练/拟合时间与能耗预测，按地图 ID 分组验证，报告欠预测尾部和“预测安全、实际不安全”比例。不同估计器共用真实低层与相同航段集合。
3. **当前队列诊断**：在 3–5 单小实例上比较最近/最短规则、深度 2/3 滚动规划与小规模参考；分别使用同一飞前预测和共享真值成本的特权诊断，检验链式价值与部分充电是否产生可重复的首动作反转。
4. **持续到达评价**：实现完整状态机与可恢复回合，在同一外生订单流、地图、硬件和决策预算下比较安全规则、稳健滚动规划及后续学习策略。参数与测试地图划分须在读取结果前冻结；当前还没有高层方法胜出的证据。

第一步的关键未决输入是**实体平台或文献参数族**：空机/电池质量、可用电量、旋翼推力功率参数、载荷范围与充电功率。若没有指定机型，应先用有出处的代表性参数族并跨参数报告结果，而非用旧项目的 synthetic energy units 假装真实 Wh。
