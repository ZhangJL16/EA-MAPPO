# 三维解析导航资格测试协议（2026-09-23）

本协议在读取资格测试结果前固定。测试属于独立项目 `uav_learning_research`，不重开已结项的 regenerative-control 研究，不训练 SAC，也不运行配送调度。

## 输入与样本

- 12 张同类未见静态地图，地图编号 0–11。每张 60×60×24，含一面高度变化的横贯墙、两块高度/位置变化的长方体和两根有限高圆柱。地图由种子 `20260923 + 1009×地图编号` 生成。
- 每图 8 条固定左到右航段：直线被障碍阻挡 4 条、直线畅通 4 条，共 96 条。每组候选起终点由独立种子 `6042026 + 7919×地图编号 + 17×组别` 生成，只依据端点净空和直线几何分类，不依据规划或飞行结果筛选。固定清单存入 `manifest.json`，失败样本保留。
- 低层明确接收事先测绘的静态三维地图；此测试不评估未知地图建图或动态障碍。起终点高度变化，障碍物高度变化；更大结构变化属于后续外推测试。
- 双积分器、`dt=0.05s`、机体半径 0.4、额外余量 0.2、水平/垂直速度上限 7/4、加速度上限 4/3；其他 CBF 参数取源码默认。每航段最多 1200 步（60 秒模拟时间），到达半径 0.8。对每个航段固定同一导航版本。

## 结果与停止规则

每条保存 `arrived`、`navigation_timeout`、`no_route` 或显式错误，统一接触数、CBF 不可行步数、介入步数、路线长度/规划耗时、实际飞行时间与轨迹、控制步和 QP 的耗时。无路与超时都计入分母；报告时按地图和直线受阻类别分层。`arrived` 但接触过不算无碰撞到达；CBF 不可行单独报告。不用 0 接触推断形式化安全保证。

逐条原子保存结果，两个航段形成首个检查点。启动健康仅检查一个受阻和一个畅通航段的结果可读、清单与源码摘要一致、无程序错误，随后停止交还用户。完整 96 条由用户续跑；不自动启动 SAC、调度或后续实验。当前阶段不根据首个检查点调参、删除样本或将 2 条当作资格率。完成后才分析是否需要换低层；任何修改产生新版本和新清单。
