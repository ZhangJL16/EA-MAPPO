# 二维避碰与返站能量双约束：待锁定的问题边界

日期：2026-09-25。用户明确表示主要兴趣转向约束强化学习中的**碰撞安全**和**始终保有安全返站电量**，并认为三维爬升可能分散这个问题。用户选择首个二维验证：学习策略选择局部航向/速度及补能动作；碰撞层与返站能量层均过滤危险动作。首个二维任务先使用**连续出现、每次只处理一个目标**，验证双约束机制后再加入取送货订单。**不要把先前订单期限和交接耗时的确认自动视为二维任务定义。**

用户随后明确：现有三维解析/规则导航、能耗和订单方案全部预留，等二维双约束做好后再回到三维。当前二维工作须放在独立模块，不能改动已冻结的三维导航与历史评测资产。

训练/评价分工已确认：训练期记录碰撞和返站失败，允许试错；正式评价期启用联合安全过滤，并同时报告过滤前的策略动作与过滤后的执行动作。二维首版**策略与安全层均可读取静态全图**，即使该图不在训练集中；这条选择只用于新的二维研究，不覆盖原三维方案的 LiDAR/低层信息边界。用户确认先检验确定性全图下的非学习规划器能否解决单目标返站问题；若规划器足够强，再决定引入能耗误差或动态障碍，不能先加难度制造学习优势。

用户确认“始终有电安全返站”是**逐执行步**要求：每步后的完整状态（至少位置、速度、剩余电量）必须仍存在一条避碰可行、能量足够的返站方案，而不是仅在选目标时检查。首版接受一条**已核验可执行的备用返站路径**作为保守证据；须单独量化它把实际上可行动作拒绝掉的比例，不要求求解全局最省电返站路线。目前没有形式化证明，不能把一个经验屏蔽器称为已证明安全。

目标状态已确认：二维首版每次仅有一个当前目标。途中因安全返站并补能时，该目标保留；充完继续，完成后才生成下一个。无目标替换或超时机制；不要沿用原三维订单的 deadline/取货承诺。

补能已确认：在站显式按时间充电，策略可随时停止并离站；站内 idle 不自动补能。二维动力学已确认：固定在既有站点与地面服务点的 **2 m 交接高度**，所有现有圆柱在此平面均成为障碍；保留此前 M100 的水平速度/加速度限制以及水平飞行功率模型，不改成地面机器人动力学。用户要求尽量与既有三维配送设定对齐，但确认**首轮仍为单目标双约束验证**；之后才按相同参数和口径接入取送货队列。

2026-09-25 新确认：策略每 **0.5 s** 更新一次期望航向/速度，碰撞与返站能量安全层每 **0.05 s** 物理步检查和必要时修正执行动作。若策略动作触发返站安全接管，沿用此前决定：执行已核验备用方案直到安全到站，再允许策略决定显式充电与离站。若核验方案本身在执行中失效，记录独立失败事件；不能凭空重置/传送到站。

2026-09-26 新确认：每完成一个目标才从该图二维可达自由空间**独立均匀抽样**下一个目标。所有比较方法共用目标随机流；另外按目标距站距离分层报告结果，分层不改变采样权重。采样器须用二维障碍净空和二维连通性验证，不能复用可能越顶的三维可达性判据。

2026-09-26 当时为 M100/1 km 二维版确认过固定 **2 h 仿真时间**主窗口、**4 h** 确认窗口；它们不是电脑实际运行时长。**随后用户已将二维首版改为合成机制环境，因此这组长窗口不再是合成首版的冻结设置，只作为 M100 后续验证的历史候选。** 合成窗口需按新尺度重定，不能把用户对 M100 窗口的选择直接外推。

2026-09-26 新确认：站内动作改为选择**目标物理 SOC**；按同一充电曲线直接推进仿真时钟至目标电量，允许部分充电，不逐 0.05 s 模拟站内空转。首版只有当前目标、没有充电期间到来的新目标，故此跳时不会跳过订单到达事件。必须在实现时统一物理 SOC、可用 Wh 和最低允许 SOC 的口径；当前 90% 可用容量假设不能把“运营 0%”误当作“物理 0%”。

2026-09-26 用户因 M100 慢充电、大地图和长逐步检查负担，明确改选：**二维首版先用较小、明示为合成的机制环境；M100/1 km 只作后续外部验证。** 返站能量安全与碰撞安全耦合仍是问题锚点。合成环境的几何、能量函数、补能速度和运营窗口须重新固定，并报告足以触发返站约束的负载区间；不能把人为缩小的合成电池写成 M100 实机电池。

2026-09-26 新确认：合成二维主实验使用透明的每步飞行能耗 `ΔE = Δt·(c₀ + cᵥ‖v‖² + cₐ‖a‖²)`，系数明确公开、逐项敏感性扫描。空中零速度仍耗 `c₀`，站内停靠 idle 零飞行耗电；充电按目标电量事件推进。这是可控制的机制模型，不是 M100 功率曲线或实测拟合。参数范围不能根据学习方法胜负反向挑选。

2026-09-26 新确认：合成首版起始场景 **200 m × 200 m、约 8 个圆形障碍、10 min 仿真运营主窗口**。保留固定 0.05 s 飞行物理步和 0.5 s 策略决策周期作为起始实现；10 min 至多 12,000 个飞行物理步，充电事件跳时会减少实际飞行步数。障碍半径与布局、无障碍目标分布和电池/充电比率须在正式比较前冻结；不得按算法排名重新筛场景。

2026-09-26 新确认：合成电池基准容量以**独立规划器估算的典型站→目标→站成本的约 2 倍**为目标，完整补电时间以**该典型往返时间的约 1 倍**为目标。需在正式方法比较前用政策无关的固定样本和规则标定具体数值、冻结并记录；之后扫描较宽/较窄预算与较快/较慢补能，不能用 RL 成绩反向选值。
2026-09-26 新确认：抽样目标若从站点满电出发仍无法完成并安全返站，则在生成时排除，并报告排除比例。候选目标先在二维可达自由空间独立均匀抽样；再用**相同的、政策无关的参考判据**筛去先天无解目标。过滤后的分布是“条件于满电可完成”的均匀候选分布，不得称为原始全自由空间均匀分布；不能按被比较策略分别筛目标。

文献边界见[全文核读记录](literature/collision_energy_coupling_20260925/README.md)：保留补能可达性的动作掩码已有先例，同时使用碰撞和能量 CBF 也已有先例。二维的价值在于压低动作与几何复杂度，以分辨“避障后回站能耗变化”是否造成两种安全约束的真实交互；若确立现象，再在三维或更真实平台检验。当前 96/96 三维导航资格、19 组能耗敏感性与内部路线估计诊断继续作为已有证据保留，但不直接证明二维双约束学习方法。

若定义双安全层，评价至少需要同时记录：统一碰撞计数、耗尽与无法返站次数、每步碰撞层/能量层干预、双层约束同时活跃的频率、可行动作交集为空的频率、因安全干预增加的行程时间与能耗、完成任务数或任务收益。`E-E_min-rho(p)` 里的 `rho` 必须使用**碰撞可行的返站路径**并说明模型误差边界；仅用直线距离不能声称必然返站。训练期试错与正式评价的失败标准必须分开。

机制实现前仍需固定：正式地图划分与样本量、每步计算时限。任务过程、地图可见范围和返站证据的边界已由用户明确：逐执行步验证可执行备用轨迹，但尚无形式化证明。安全过滤无解须报告失败，不能制造一个不存在的可行返站动作。不能因已有 CBF 论文而声称简单叠加两约束有新颖性；也不能因二维模型通过便声称真实四旋翼安全。
