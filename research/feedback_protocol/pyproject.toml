[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "feedback-protocol-lab"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
fpl = "fpl.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
