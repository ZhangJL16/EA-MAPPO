# 区分“能耗估计难”与“订单/补能决策难”的最小实验计划

日期：2026-09-23。性质：**新研究的问题识别方案，尚未批准具体参数或启动运行**。旧 `mappo` 的 regenerative-control 分支已结项；本方案不在旧任务上续跑、重训或套用其 98% kill test。主评价指标见 [评价协议](EVALUATION_PROTOCOL.md)；核读的论文与 PDF 见 [文献目录](literature/fair_comparison_20260923/README.md)。

## 先用一个例子解释滚动规划

在某个高层决策时刻，无人机看见**当前**订单、站点、电量和各候选飞行的时间/能耗预测。它暂时枚举若干可能序列，例如 `取A→送A→取B→送B→充到65%` 与 `先到站充到80%→取B→送B`，按预计准时完成数、安全可行性与时间选一条；**只执行第一步**。下一高层时刻，用实际位置、电量和新到订单重新算。它不需要知道未来订单，也不要求能耗预测完全正确。“滚动”指这个执行—观测—重算的过程。

所谓规划失败可以来自四个不同位置：路线时间/能耗预测错误；预测合理但安全余量处理错误；搜索深度/时间不够；目标或动作模型漏掉了送达后位置、载荷、部分充电、承诺订单。必须分开测试，不能一看到 MPC 失败就归因于“它不会智能规划”。

## 实验 1：先量化可部署能耗预测到底多不准

**问题**：单段取货、载货送达和回站的能耗误差有多大？误差是否足以翻转“能否履约并安全到站”的判断？

**设计**：首选[解析三维导航＋CBF](NAVIGATION3D.md)作为共同低层，完成多地图资格后冻结其版本及物理能耗模型；用多张训练地图采集航段，完整测试地图 ID 从未进入拟合。分别覆盖空载/载货、不同起终点高度和绕障结构、不同起始 SOC、向任务点及向各站飞行。部署预测器只用合法观测及路线摘要，不读取障碍物真值。预先生成候选航段并执行，不能只收集某策略成功选择的航段；耗尽或 timeout 的未完成航段记为删失/失败，不把已消耗电量错当完整能耗。若需测其完整消耗，只能另开明确标记的诊断仿真。

**比较**：简单距离/高度模型；用可部署路线长度、载荷和**飞前可得的预计运动摘要**的增强模型；以及可校准的高分位能耗预测。实际飞行后的速度、加速度只能作标签/误差分析，不能偷作飞前特征。后两者的复杂度由实际训练地图数据决定，不预设神经网络一定优越。

**输出**：时间与能耗 MAE/RMSE、欠预测比例及欠预测尾部、条件分位数覆盖率、预测“任务+送达+站点”可行但实际失败的比例，按载荷/地图/距离分层。另记录单次预测和路线查询时延。

**解释**：若点预测系统性偏低而上分位数预测可控制 false-safe，先把共同估计接口改成不确定性输出；这不能单独证明学习型调度优于鲁棒规划。若连可部署区间也不能覆盖关键失败，应先修数据/物理模型或承认安全保证边界。

## 实验 2：隔离“链式价值与部分充电”是否真的需要前瞻

**问题**：即使路线成本准确，最短/最近订单是否仍会系统性选错？错误是两步、三步搜索就能解决，还是需要更长的序列价值？

**设计**：先做小而透明的静态当前队列诊断。用完整真实 simulator 状态和同一低层飞行机制构造 3–5 单的小实例，含取货、送达、载荷、deadline、至少一个充电站和可选部分充电；在这一诊断窗口内暂停**新订单生成**，只为隔离当前队列的序列效应。航段真实成本或反事实分支只在诊断中提供给所有对手，明确标为特权信息。预先固定三类案例：顺路链、送达后靠近充电站、相同 SOC 但不同队列几何；同时纳入随机生成的未筛选案例，不能只展示手工成功例。

**比较**：最近可行单；一次只看当前订单的 greedy；深度 2、3 的当前队列滚动规划；小实例枚举/动态规划参考。所有方法使用相同真实成本和同一安全定义。分别允许和禁止部分充电，确定其独立作用。

**输出**：各方法准时完成数、第一动作是否发生反转、何时选择补能、补到多少、每次求解时长。保存每个反转状态的当前队列、SOC、位置、预测/真实成本和后续轨迹；重点看“较远任务先做反而多完成单”是否反复出现，而不是只报总体均值。

**解释**：如果深度 2–3 已追平小实例参考，那么“需要长程学习规划”暂时没有证据；若 greedy 也追平，当前队列的链式价值在这些实例上不显著。若复杂规划有收益但超出允许决策时延，学习可被研究为**摊销搜索**。这个静态诊断不证明未来随机订单的价值。

## 实验 3：在真正在线任务流中做同信息、同预算比较

**问题**：随着订单到达、队列容量变化和充电耗时，鲁棒滚动规划能否处理实验 1 发现的误差、实验 2 发现的链式价值？

**设计**：实现新项目的单无人机取货→送达状态机，订单持续外生到达；未取货可不接/过期，已取货必须履约。各方法共用训练/测试地图切分、外生订单流、低层、安全层、可部署预测器和路线 API。第一轮只比较安全最近单、点预测 rolling-horizon/MPC、使用同一预测区间的鲁棒 rolling-horizon/MPC；规划期间物理时钟前进，仅执行当前动作，并在下个固定高层 tick 重算。只在基础现象明确后加入学习策略；其主表必须与**最强的可部署规划基线**比较，而非仅与最近邻比较。

**输出**：按[评价协议](EVALUATION_PROTOCOL.md)报告窗口内准时完成数、耗尽/未履约/回站失败、订单拒收与过期、统一碰撞数，以及完成数—决策时限曲线。配对比较在未见地图上进行，并按负载、电池、队列长度、充电站几何分层。诊断副表让各方法共享真值航段成本；另有少量完美未来订单参考上界，但不能进入公平在线主表。

**解释矩阵**：

| 观察 | 暂时支持的解释 | 下一步 |
|---|---|---|
| 点预测 MPC 失败，鲁棒 MPC 解决大部分安全问题 | 主要是能耗不确定性处理，而非学习规划必要 | 对比相同预测区间下的学习策略，检验其是否还有准时单增益 |
| 真值成本下 greedy 输、浅层 MPC 胜 | 当前队列有前瞻价值，但短搜索已足够 | 学习方法必须胜于浅层 MPC，或显著节省在线算力 |
| 真值成本下浅层 MPC 仍输给小实例参考 | 搜索深度/动作表示存在缺口 | 检查更深规划在真实时限内是否可行，再定义学习目标 |
| 所有方法用真值成本差距小、部署预测下差距大 | 估计器与安全余量主导 | 先研究预测与稳健决策的分解，不把差距直接命名为调度智能 |
| 学习方法在同预测器、同安全层、同预算下跨未见地图多完成单 | 可能有可学习的在线决策优势 | 进一步做动作反转、部分充电和信息消融，排除泄漏/调参偏差 |

## 实现与停止规则

先完成实验 1 的测量定义与实验 2 的可执行小实例；它们可以帮助理解研究问题，**不作为无限延长的训练前门槛**。实验 3 的各方法和时限在读取测试地图结果前冻结。只运行防止无效/不可恢复运行所需的针对性检查、一个新路径 smoke 和首 checkpoint 健康检查；运行可恢复，首 checkpoint 后交还用户，不自动监控到完成或自动推进训练。

尚未定的 `T`、高层周期、决策预算、deadline slack、地图数、载荷范围、充电速率、低层控制器实现以及安全风险样本量，必须在新项目环境具体化时决定。不能把旧 1000 条单航段 calibration 当成这个取送货/载荷/部分充电问题的验证。旧 B5 的 227/270 次耗尽及后续分解显示当时存在预测误差、fallback、站外等待和返航 false-safe 等不同来源；它只说明需要拆因，不预测新项目的优劣。
