# 首版飞后能耗计量器：公式、证据与未定参数

本文件记录 [`delivery_1km/energy.py`](delivery_1km/energy.py) 的**参数化实现**。它尚不是 M100 实测验证、已冻结主实验参数，也不是飞前能耗预测器。用户确认：首轮只主张模型内结果、参数敏感性；不模拟外部风，但保留前飞空气动力效应；冻结轨迹控制，载荷只改变功率与能力检查。

## 已有出处的结构

- 厂家给出 M100+TB47D 名义电量 99.9 Wh，以及额外载荷 0、0.5、1.0 kg 时悬停时间约 22、17、13 分钟。本实现用 `99.9 Wh / 悬停时间` 得到三个**等效平均功率锚点**，在 `m^1.5` 空间插值。实际计算时，同一未知可用电量比例同时乘入锚点功率与电池可用容量，确保单纯改变该比例不会凭空改变厂家悬停时间。它不是逐秒电功率实测，也没有证明 99.9 Wh 可全量使用。[DJI M100 官方规格](https://www.dji.com/matrice100)。
- [Rodrigues et al., *Patterns* 2022](https://doi.org/10.1016/j.patter.2022.100569)用 M100 飞行数据发现以诱导功率、质量约 `m^1.5` 表示的平均能耗模型在其测试航线有效；这不验证本项目三维频繁加减速。其全文副本在 [`literature/platform_energy_20260923/`](literature/platform_energy_20260923/README.md)。
- [Gao et al., *China Communications* 2021](https://doi.org/10.23919/JCC.2021.07.020)对旋翼机前飞速度—功率关系提供模型和实测支持。这里借用诱导流随水平速度变化、剖面功率随速度平方变化、寄生阻力随速度三次方变化的**结构**；其系数并非 M100 的实测拟合。上升机械功率另用 `m g max(v_z,0)/η`，未模拟下降能量回收。

每步以已执行的加速度 `a`、水平速度 `v_xy` 和假设阻力面积计算 `D = ρ C_D A ||v_xy||²/2`，再计算所需推力向量 `F_xy = m a_xy + D v_xy/||v_xy||`、`F_z = m(g+a_z)`；诱导项随 `(||F||/mg)^(3/2)` 变化。阻力的功率为 `D ||v_xy||/η`。`step_energy` 输出瞬时功率、推力、倾角和违反约束的类别，`account_flight_trace` 按实际控制步长积分为 Wh。终点到达时模拟器把速度强制置零；这次软件状态修改不算电机瞬时脉冲。接触修复同样不是可物理解释的飞行机动，接触必须另行报告。

低层仍是冻结的双积分器；这里把抵消无风条件下机体阻力所需的额外推力算入计量和可行性检查，等价于假设内环能够补偿阻力而维持原轨迹。**这不是**在仿真动力学中实测了阻力扰动。若该假设对主结论敏感，需要单独重建含阻力的动力学并重新做导航资格。

能力检查包含厂家 3.6 kg 最大起飞质量、35° 最大倾角、5 m/s 上升与 4 m/s 下降速度。厂家资料没有给出可直接核验的 M100 电机总推力上限，故额外的 `thrust_headroom_over_mtow` 是显式假设，检查 `F ≤ headroom × 3.6 kg × g`。此处输出仅是“**假设模型内可执行**”；不能当作实机安全证书。来源：[DJI M100 规格](https://www.dji.com/matrice100)、[官方 M100 手册](https://download.dji-innovations.com/downloads/dev/Matrice/en/M100_User_Manual_v1.4_en.pdf)。

## 运行前必须冻结的未测量量

`EnergyAssumption` 要求显式提供：可用电量占标称比例、厂家悬停等效功率倍率、诱导/剖面功率份额、桨盘直径、旋翼桨尖速度、等效阻力面积、推进效率、附加电子设备功率、推力余量。它们没有在模块中伪装成厂家默认值。测试里的 `illustrative_test_only` 仅检查接口与单调性，不能作为主实验配置；此前一条航迹的执行检查同理。

飞后标签从实际每步速度和加速度计算。飞前估计器只能使用出发前的路线摘要、载荷、SOC 等合法信息，不得读取该航段实际轨迹。能耗模型参数、训练/验证/测试地图与敏感性范围需要在批量生成标签和读取结果前经用户审阅并冻结。之后再做欠预测、false-safe 与路线成本审计，才有条件进入订单/补能诊断。
