# 冻结 SAC 原生分布新地图测试协议（2026-09-23）

在查看本次结果前固定：使用旧 SAC checkpoint SHA-256 `fb79482ae7d832b40137ff1a080ee84912eabb95867e118aca80c846f6ae62be`，原 `FrozenNavigator`、原 LiDAR 观察和 HOCBF 过滤器；不更新网络，不改碰撞恢复或惩罚。4 张新随机地图使用种子 720260921–720260924，各 24 根贯穿全高圆柱；每图用独立固定种子生成 4 对合法起终点，共 16 条。每次从静止起点开始单航段，虚拟大电池防止能耗截断，最多 4000 个策略步。记录到达、超时、统一接触、HOCBF fallback、模拟飞行时间、synthetic 能耗和墙钟时间；失败保留。两条任务形成首个检查点，然后遵循仓库 `AGENTS.md` 的启动约束停下交还用户。运行产物按任务原子保存，可以从完成任务之后继续。

这是一项**原生条件下的新地图泛化检查**。旧 SAC 原生环境是 4000×4000×400、全高圆柱、到达半径 5 米、水平速度上限 20；解析导航资格测试是 60×60×24、有限高圆柱和长方体、到达半径 0.8、水平速度上限 7。两项测试的成功率不能直接排名。历史固定单图 1000 对的 988/1000 到达率单独作为先前证据，不能并入本次 16 条。
