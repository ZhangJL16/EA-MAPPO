# 本地解析导航资格测试结果

2026-09-23。独立项目 `uav_learning_research`，冻结协议见 [NAV3D_QUALIFICATION_PROTOCOL.md](NAV3D_QUALIFICATION_PROTOCOL.md)。从 2/96 检查点在本地续跑至 96/96；未训练网络、未启动配送调度，也未修改已结项仓库的实验。原始清单、检查点、逐条 JSON 和每条飞行轨迹位于 [`artifacts/nav3d_qualification_20260923/`](artifacts/nav3d_qualification_20260923/)。固定源码及协议 SHA-256：`247cb639eb43c16e31a388c74af80fc486722387d428f4cd0773e8c56f6aad6a`，分析时与清单重新核对一致。96 个结果 ID 与清单完全一致，无重复或缺项。

| 指标 | 本地结果 |
|---|---:|
| 到达 | 89/96 = 92.71%；Wilson 95% 区间约 [85.71%, 96.42%] |
| 直线受阻组 | 47/48 到达 |
| 直线畅通组 | 42/48 到达 |
| 导航超时 | 7/96；6 条畅通、1 条受阻 |
| 规划器无路 | 0/96 |
| 统一接触 | 0（96 条轨迹合计）；不是安全保证 |
| 控制器报告不可行的步数 | 8007/27139 个控制步；其中超时航段 7394 步 |
| 成功航段中曾报告不可行 | 35/89 |
| 成功航段模拟飞行时间中位数/P90 | 9.70/13.86 秒 |
| 航路规划墙钟时间 P50/P95/P99 | 0.0649/0.1572/0.1615 秒 |
| 每控制步墙钟时间 P50/P95/P99 | 0.580/0.725/0.902 毫秒 |
| 每步 QP 计算时间 P50/P95/P99 | 0.246/0.333/0.468 毫秒 |
| 96 条任务墙钟时间总和 | 23.64 秒（当前本地机器） |

7 条超时航段：`m00_clear_01`、`m05_clear_00`、`m05_clear_02`、`m06_clear_01`、`m10_clear_01`、`m10_clear_03`、`m11_blocked_01`。逐条轨迹显示它们在横贯墙的左侧附近停止，最后 100 个控制步位移均为 0；末点距目标约 28–32 米。对两条代表性失败状态重放控制器，分别得到 `emergency_brake_after_primal infeasible` 与 `emergency_brake_after_finite_hold_clearance_failed`。这支持“局部控制陷入边界状态”的诊断，而不是规划器无路。部分轨迹略微侵入 CBF 的额外 0.2 米裕量，但未发生按机体半径计的物理接触。`controller_infeasible_steps` 统计的是控制器返回 `feasible=False` 的步数，可能包括初始 CBF 条件失效或有限保持检查失败，不能全部解释为 QP 求解器直接无解。

**判断：此版解析低层尚不适合作为配送研究的冻结共同导航器。** 它速度足够快，在此测试里没有接触，但 7.29% 的航段超时，且大量不可行步集中在墙前停滞。完整结果保留为 v1 基线，不依据结果删样本或覆写。下一版若继续解析路线，应在独立版本中解决临近障碍的跟踪余量与陷入时恢复，再用相同已冻结航段做成对复测，并另用新地图评估泛化；不要把同一 96 条上修正后的成绩称为独立测试。当前测试只有静态已知地图、双积分器与导航目标点，不能说明动态障碍、真实四旋翼能耗、取货配送或补能调度效果。
