# 冻结 PPO 的站内充电失效与隔离诊断

日期：2026-09-26。范围：本地二维合成环境，冻结 PPO 的验证图 32–39。
本报告不改变原始训练、原始验证结果、碰撞恢复机制或共同安全层。

原验证运行的 3 个 PPO seed 均完成 8/8 张图，每图 600 秒。逐条复核
`events.jsonl` 与 `summary.json` 后得到：

| seed | 完成目标总数 | 显式充电 | 站内出发被拒 / 总决策 | 比例 |
| --- | ---: | ---: | ---: | ---: |
| 101 | 7 | 0 | 8506 / 9112 | 93.35% |
| 202 | 14 | 0 | 8635 / 9346 | 92.39% |
| 303 | 8 | 0 | 8510 / 9144 | 93.07% |

虽然部分模型输出过充电动作 ID，但日志中没有一次在站内成功执行充电。
例如 seed 101、map 32 在低电量站内持续输出动作 9；这个动作在站内
按共同动作适配器解释为向目标出发，因缺少可执行返站证据而反复被拒。
这足以证明**当前冻结 PPO 的站内补能行为失败**，但不能单独证明其空中
导航能力、训练算法类别或双约束研究问题本身失败。

为隔离这个瓶颈，新增 `diagnostics2d/dock_supervision.py`：保留 PPO 所有
空中动作；在站内，保留任何有效的显式充电选择；只有拟执行出发而当前
电量不足以覆盖共同参考往返成本加固定 5% 容量余量时，改为最小可用
充电档位。这个参考估计不是安全证明，原 CPU 安全层仍核验每一步。
逐决策日志同时记录网络提议与实际执行动作。它是**监督干预诊断**，
不是纯 PPO，也不能拿其收益充当学习策略的主表成绩。

`diagnostics2d/dock_matrix.py` 冻结 3 个模型 SHA、8 张验证图、校准与源码，
支持最多 8 个子进程并行、逐图断点和原位恢复。没有接触测试图 40–55，
也没有启动新的训练。现只执行了 `--startup-only`：seed 101、map 32 的
前 40 个决策（两个 20 决策断点）一致恢复；第 27 个决策把 PPO 的动作 9
改为充到 75%，出现首次显式充电。此时完成 1 个目标、0 次出发被拒、
0 次碰撞/耗尽；**尚未完成 600 秒，不能推断最终吞吐量改善**。

当前矩阵目录为本地
`artifacts/ppo_dock_supervision_validation_20260926/`；顶层 manifest
SHA-256 为 `45df92582b1ea725e1136c589cad3050ce6fd36128b1484c2f2745075bd01afa`。
首作业第 40 决策 checkpoint SHA-256 为
`57fbe303402e4747e2749963916aa970053fa96da0e34f0c941dcfd8fc3e4c8c`。
源码针对性 6 项测试通过；与先前未完成的原始二维矩阵相比，旧源码
SHA 差异为 0，因此该诊断没有破坏原矩阵的恢复条件。

续跑完整 24 个验证作业的单行命令：

`cd /home/zjl/uav_learning_research && python3 -m diagnostics2d.dock_matrix --output artifacts/ppo_dock_supervision_validation_20260926 --workers 8`

解释规则：若监督干预明显恢复完成数，下一步研究模式条件充电动作学习
与训练状态覆盖；若仍低，继续拆空中航向/速度与安全层接管。无论哪种，
都不能把这个辅助策略与 H4 直接作为公平的“学习胜过规划”主比较。
