# 解析导航 7 条超时的事后诊断

本文件只分析已冻结的 [96 条资格测试](QUALIFICATION_REPORT_20260923.md)，不修改 `nav3d` 源码、manifest、参数或原始结果。2026-09-23。

## 直接证据

全部 7 条超时轨迹最终停在横贯墙左侧，最后 100 步位移为零，统一接触均为 0。6 条被分类为“起终点直线畅通”，规划路线仅有起点和终点；1 条起终点直线受阻，路线有一个中间航点。`VisibilityPlanner.plan()` 仅检查路线线段与膨胀障碍不相交；`CBFController.nominal_acceleration()` 对目标点的水平距离和竖直距离分别设定期望速度，未跟踪该线段的中间高度或为越墙保留高度。

例：`m00_clear_01` 的起点高 18.74 m，终点高 14.01 m，墙顶高 14.94 m。计划直线到墙前边界时高约 16.64 m，几何上可过；实际轨迹在墙左边停于高约 14.11 m，低于墙顶加 0.6 m 膨胀后的 15.54 m。控制器降高过早，然后必须在墙前刹停。`m11_blocked_01` 的计划航点从起点 `[3.05,35.41,18.87]` 直连 `[49.72,33.71,7.30]`，也依赖在穿墙时仍保持足够高度；逐轴目标速度控制不保证沿此三维线段跟踪。

对每条失败轨迹的首个 `feasible=False` 状态，重建源码里的全部线性 QP 约束，并使用 SciPy HiGHS 独立解零目标线性可行性问题：**7/7 均不可行**。其中 6/7 有单个障碍约束要求的法向加速度超过水平 4 m/s² 上限：首次不可行所需约 4.009–4.387 m/s²；剩余一条为约束联合冲突。故首个失败不能归咎于 OSQP 单独的数值错误。QP 不可行也不等于物理上完全没有安全动作；它说明**当前选定的 CBF 条件、速度/加速度界与状态不兼容**。紧急制动保住了机体接触指标，但没有恢复路线进度。

代码依据：`nav3d/planner.py` 的直线判定和图平滑；`nav3d/controller.py` 的逐轴目标速度、CBF 约束、OSQP 投影与不可行制动；`nav3d/simulation.py` 将路线航点作为控制目标。当前只检查单个采样周期的保持轨迹净空，没有对以后的 QP 可行性或航路跟踪给出保证。`controller_infeasible_steps` 包含初始条件失效、有限保持检查失败等，不应全部解释为求解器原始无解。

## 对设计的含义

**QP 安全过滤这个方法本身没有被推翻；本版是参考轨迹、名义控制、输入受限 CBF 和恢复逻辑没有配套。** 下一版应先让名义控制跟踪经过墙顶的三维参考线段/走廊，在越墙前限制提前降高；再加入带实际加速度上限的预刹车或多步可行性处理；若首次 QP 无解，应按可行的航路恢复，而不是无限原地制动。独立版本需要记录每步失败原因、障碍法向余量、路线横向/竖向跟踪误差和恢复次数。用原 96 条做成对回归，再在未看过的新地图测试；不能在原样本上修复后将其当作独立泛化成绩。

相关理论背景：[Xiao、Belta、Cassandras 的 CBF-QP 可行性论文](https://arxiv.org/abs/2011.08248)明确讨论紧控制界与高相对阶约束的可行性；[Xiao 等人的不期望平衡点分析](https://arxiv.org/abs/2104.14895)讨论 CBF-QP 的非目标平衡点；[Taylor 等人的采样数据 CBF 工作](https://authors.library.caltech.edu/records/v0rk2-qym53)讨论连续时间设计与离散执行之间的差距。这些论文解释了为何“QP 求得快、动作安全”本身不等于全局可达或每步可行；本报告的具体原因仍以本地源码和轨迹为依据。
