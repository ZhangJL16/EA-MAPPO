# 飞前航段估计的预备诊断协议

日期：2026-09-25。在读取本轮估计结果前固定。输入仅使用已完成的 96 条 1 km 冻结导航航迹，以及经用户确认的基准能耗假设。每图 8 条、共 12 图；这些地图已经用于导航资格与能耗诊断，故本轮是**内部跨地图预备诊断**，不能当作未来独立测试地图的最终泛化结果。没有新飞行、订单或神经训练。

比较两个普通线性基线：

1. `direct`：截距、起终点欧氏距离、起终点高度差绝对值；
2. `route`：截距、低层飞前规划航线总长度、计划总爬升、计划总下降、计划航段数。

路线由低层读取静态地图并生成，只给高层返回以上摘要；高层/拟合器不得读取障碍物坐标、半径、地图 ID 或飞后轨迹。地图 ID 只用于交叉验证分组。能耗每种载荷单独拟合；时间每条真实航迹只计一次。逐张地图留出，其他 11 张图拟合，再预测这一张。拟合普通最小二乘并将负预测截为零；两个特征组、算法、载荷及分组规则不随结果更改。

报告各模型的 MAE、RMSE、R²、欠预测比例、正残差的 P90/P95/P99 和最大值；保留每个航段的配对预测与原始来源 SHA。只做飞前航段模型的误差审计，不计算订单完成率、安全证明或“预测安全、实际不安全”的整链比例。后两项需订单链、SOC 与独立测试地图。
