# 下一阶段：本地二维比较与服务器独立三维

日期：2026-09-26。这里是新项目的实施顺序；旧 `/home/zjl/mappo` regenerative-control
分支继续结项。本机二维 GPU-MPC 的 48 个作业已经完成，目前未发现相关
本地运行进程。用户说明服务器上正独立开展三维工作；本机没有核查服务器
进程、完成度或具体结果。本计划不自动启动新的长实验。

## 当前结论边界

二维合成环境的 48/48 GPU-MPC 作业已完成。冻结测试图 40–55 上，H4 为
138/16=8.625 个目标/图，H8 为 85/16=5.3125；二者均记录零碰撞、零耗尽。
这只说明当前近似 GPU 候选搜索的 H4 优于 H8。此前 PPO 的验证图均值为
0.875、1.75、1.0，且没有冻结测试图配对结果。CPU-H2 的冻结测试配对为空。
因此“学习胜过强 MPC”目前无证据。

现有 GPU-MPC 只从 9 个目标方向/速度中挑选飞行动作；飞行中不主动选择
动作 9 返站，停靠时采用固定充电阈值。下一轮必须让规划器明确考虑
返站与充电动作，再讨论学习是否有决策优势。保留当前 H4/H8 原结果，
不回写历史日志。

## 本地二维：最小决定性比较

1. 用现有事件日志量化 H4/H8 的首次动作分歧、目标完成时间、返站接管、
   充电和决策时延；将“候选排名误差”“返站/充电动作缺失”分开报告。
   这一步只读，不是新增训练门槛。
2. 实现一个同信息、同动作集的规划对照：目标方向/速度、主动返站、
   站内 50/75/100% 充电都可选；执行动作仍由完全相同的 CPU 安全层核验。
   在验证图 32–39 选定规划深度和在线计算预算。至少同时保留
   `route_full`（连续动作解析路线基线）和旧 H4（离散动作旧实现）作为锚点；
   分开说明它们与 PPO 的动作空间差异。规划器的候选分数要显式计入完成目标、
   充电耗时与返站机会成本；不能仅用航向目标的短时位移代替吞吐量。
3. 冻结所有方法后，把现有 PPO checkpoint（各训练 seed）、`route_full`、
   旧 H4 和完整动作规划器放在**同一新测试图 56–71** 上逐图配对。
   40–55 已用于发现现有缺陷，不能作为新规划器的未见确认集。
   每图使用相同外生目标随机流、600 秒仿真窗口、相同安全层和地图信息。
   报告完成数、碰撞/耗尽、返站失败、两层过滤与接管、充电、每次决策
   p50/p95 墙钟时延及总计算成本。完成数的置信区间以地图配对差构造；
   多 PPO seed 分开报告，不能只挑最优 seed。
4. 预先执行判断规则：如果冻结 PPO 仍不及强规划器，终止“确定性全图下
   学习胜过强 MPC”的主张；如实保留负结果，然后另立能耗不确定性/动态
   障碍问题。若学习超过规划器，先检查算法是否共享动作、信息、预算和
   安全层，再用更多未见地图确认；零失败只归于整套执行系统，不能直接
   归功于策略网络。离散动作 PPO 与规划器作严格同动作比较；连续动作
   `route_full` 作为另一个强解析参照单独解释。

这是一套比较，不是用延长训练时长替代方法诊断。新运行须可恢复；只做
针对性代码检查、新路径的微型 smoke 和首 checkpoint 健康检查，然后交还用户。

## 服务器三维：独立并行，不改变本地二维主线

本机继续分析和推进二维双约束。服务器侧由独立人员开展三维工作，具体
运行内容和状态以服务器证据为准，不能用本机二维状态推断服务器已经停止
或完成。已有三维代码与 `SERVER_ARM_3D_HANDOFF_20260926.md` 是交接资产；
本地 1 km 静态导航 96/96 到达、零接触，不能把它写成三维配送结论。

对服务器三维线，我建议首先审计**冻结导航＋实际飞后能耗＋每步可执行返站备用路径**
的连续单目标原型，并在多图、载荷和已冻结的 19 组能耗假设上审计：
假安全率、返站证据失效、干预次数及求解时延。部署侧预测器只能使用
飞前可得路线摘要/合法观测；完整几何真值只用于物理与明确标记的诊断。
三维取货→送达队列、期限和部分充电状态机按已确认协议接入，
不把二维单目标结果误称为三维订单调度。服务器的新三维矩阵需先有
按图配对、独立输出目录、内存上限和断点协议；ARM 适配不能改写
冻结导航控制或历史资格数据。

## 研究方向的停止线

确定性全图任务若被强解析方法解决，就报告其边界；下一问题应清楚引入
现实中确有的能耗模型误差、局部感知或动态障碍，并检验它们如何影响
“避障后仍能安全返站”，而非单纯加大地图/训练时长。新的学习方法必须
在同一信息和安全层下提供完成数、计算时延或稳健性方面的可重复优势。
