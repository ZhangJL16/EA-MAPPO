# v2 解析导航的修复与评测协议

2026-09-23，在 v2 批量结果之前记录。v1 的源码、96 条原始结果与 SHA 不变。v2 用三维航段跟踪名义控制替代“只朝终点逐轴调速”，保持原物理接触修复、CBF/QP 安全约束、dt=0.05 秒、机体半径 0.4 米、额外 CBF 余量 0.2 米、速度/加速度上限和 60 秒模拟超时。规划时把余量扩至 1.0 米以给跟踪误差留出空间；不能把这一变化误称为求解器修复。中间航点半径由 0.8 米收紧到 0.4 米。

先在原已见的 96 条航段成对回归，全部失败继续保留，并与 v1 比较到达、统一接触、无路、CBF 不可行步、飞行时间及规划/控制耗时。再在独立同类新地图上检查泛化：地图编号 100–111，经原生成器的独立种子得到 12 张新地图，每图 4 条几何直线受阻和 4 条直线畅通，共 96 条；每组航段使用独立种子，端点只按几何净空和直线是否畅通筛选，不按导航结果筛选。两批结果不合并为一个成功率。

诊断修复的基本目标：原 7 条超时不再停在墙前；原 96 条的到达率提高且不能靠碰撞换取；新 96 条报告全部失败类型。单批 0 接触不构成概率或形式化安全保证。本 v2 不是新神经训练，也不是配送/能耗实验。逐条保存轨迹和结果，可从任务边界恢复，先核查第一个检查点。若需要改动 v2，生成新版本和新 manifest；不能覆写已读结果后称其独立测试。
