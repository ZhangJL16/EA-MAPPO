# 单无人机动态配送：评价指标约定

状态：**用户于 2026-09-23 接受的主评价方向；研究假设、具体实验规模与数值阈值尚未确定。** 本文件属于独立新研究资料，不启动或重开旧 `mappo` 实验。[问题识别实验计划](DISCRIMINATING_EXPERIMENTS.md)；依据与 PDF 见 [文献目录](literature/fair_comparison_20260923/README.md)。

## 1. 计分对象

每个测试回合有固定运营窗口 `[0,T]`。外生订单流在运行前生成并冻结：订单 `i` 具有生成时刻 `r_i<T`、取货点、送达点、载荷和从 `r_i` 起算的截止时刻 `d_i`。订单流与策略无关；每个策略面对同一地图、起始状态和外生订单流。队列最多 10 单，已取货的在途订单占一个名额。队列满时新单被拒，但仍计入该回合的生成订单总数。

未取货订单到截止时刻即过期并可从队列移出。取货即产生履约义务：即使已超时，也必须送达；途中可为履约补能。`T` 后不再取新货，但仍完成已取货订单并安全回站。`T` 后的服务结果和耗时列入清理阶段指标，不回填运营窗口得分。

## 2. 主指标

令 `t_i^drop` 为订单实际送达时刻，未送达则为 `+∞`。准时完成数定义为

`C_on = Σ_{i:r_i<T} 1[t_i^drop ≤ d_i 且 t_i^drop ≤ T]`。

主表报告每回合 `C_on`、`C_on/(T/3600)`（若 `T` 用秒，单位为单/小时），以及 `C_on/N_arr`，其中 `N_arr = #{i:r_i<T}` **包含队列满时拒绝的订单**。每种策略配对使用同一回合，主比较量是学习方法减去当前队列 rolling-horizon/MPC 的 `C_on` 差值。不得把迟到送达算作准时完成，也不得因策略拒绝远单而缩小分母。

主目标不增加远单权重、补能惩罚或人工组合分数。平均订单完成时间是次指标，不能用它替代 `C_on`。

## 3. 安全及履约门槛

正式评估中，电量耗尽、取货后未送达、清理阶段未能安全回站分别计数并报告有失败的回合比例。**部署可接受性的判定先看这三项，再比较准时完成数。** 若观测到任何一次失败，该策略不能被称作通过本次安全筛查；若没有观测到失败，只能称本样本未发生，并报告置信区间，不声称风险已被证明为零。

碰撞按既有冻结机制记录**每个 policy step 至多一次的统一 collision count**，并由总次数为零导出无碰撞到达率；不拆成边界/障碍物碰撞统计。导航 timeout、候选动作被能量层屏蔽次数、飞行中应急接管次数另列。不能把这些安全量只折合进平均 reward。

## 4. 订单结果分解

每张地图、每个回合保存全部生成订单的最终互斥状态：

1. 运营窗口内准时送达；
2. 运营窗口内迟到送达；
3. 运营窗口后清理阶段送达；
4. 队列满时拒绝；
5. 未取货即过期；
6. 到 `T` 仍未取货且尚未过期；
7. 已取货但因失败未履约。

这些类别之和必须等于 `N_arr`。另外报告从订单生成到送达的均值/P90、晚到时间均值/P90、按绕障距离分组的准时完成率、每组拒单/过期率、队列长度分布和当前 SOC 分层结果。远单分组只作分析，不改变主计分。

## 5. 补能与效率分解

报告补能次数、总补能时间、每次离站 SOC 分布、部分充电比例、每准时完成单实际能耗、载货/空载飞行时间和清理阶段耗时。能耗指标是诊断量；不能用更低能耗抵消更少准时订单或电量耗尽。

## 6. 决策时间与公平性

一次高层决策延迟从观测交付开始计至可执行动作返回，包含路线/能耗查询、特征构造、模型推理、组合搜索、安全筛选和同步开销。另报离线预处理与缓存占用。每种方法在同一硬件、线程/设备配额、地图缓存规则及每次决策时限下运行；超时触发事先固定的共同安全兜底，并计入其完成数。若按真实在线执行，求解期间物理仿真时钟继续前进，低层延续既定安全动作，不可免费暂停世界。

报告单次延迟的 P50/P95/P99、最大值、超时率，以及多个预先指定时限下的 `C_on`—时限曲线。部署时限必须小于高层决策周期；周期和具体预算应在读取测试结果前冻结。若学习方法只在完全忽略规划时间时领先，应明确报告。

## 7. 主表与诊断表分离

**主表在线对手**：安全最近/最短单规则；同输入同预算的当前队列 rolling-horizon/MPC；学习型高层方法及同观察的简单学习基线。所有方法共用低层、可部署路线估计接口和能量安全层，且只看当前订单。训练/验证/测试以完整地图 ID 切分；不同订单种子不使训练地图变成未见地图。

**诊断表**：真实执行时间/能耗替代预测值、特权未来订单的小规模上界、能量估计误差审计、去掉部分充电或链式价值信息的消融。特权信息不得进入主表；基线也应获得同一诊断信息，以便分离预测误差和调度误差。

同一地图上多个订单流构成相关样本。保留每个地图/回合的配对差值；跨地图泛化结论以未见地图为统计单位，报告分地图结果和配对置信区间。训练随机种子与测试订单随机种子分开记录。预先列明负载、电池、充电站配置和地图类别，不按方法胜负挑选 regime。

## 8. 尚待实施前确定的数值

`T`、高层决策周期、每次决策时间预算集合、训练/验证/测试地图数、每图订单流数、deadline slack 规则、清理阶段上限、失败风险筛查所需样本量，以及统计检验/置信区间方法。这些数值尚未由用户授权或现有数据推出；必须在查看新项目测试结果前定稿。现有 1000 次导航 calibration 不能替代动态配送回合的评价样本。
