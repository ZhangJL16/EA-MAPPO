# 解析三维导航＋CBF：独立首版

日期：2026-09-23。用户选择先尝试解析导航；只有该方向在后续多地图评测中效果不足，才考虑用旧 SAC 重新训练。实现位于 `nav3d/`，**不是已结项 `mappo` 源码的修改**，也没有启动新的 SAC 训练或配送实验。

## 目前能做什么

1. `geometry.py`：有界三维空间、长方体与有限高度的圆柱体；可按机体半径/额外余量保守膨胀；支持精确的线段与膨胀盒、膨胀有限圆柱相交判断。圆柱沿半径和高度同时膨胀，角部是保守近似。
2. `planner.py`：确定性三维可见图。生成绕障侧面、障碍物上方及允许时下方的航路候选；检查整段是否避开膨胀障碍，再做最短路和平滑。失败时明确抛出 `NoRouteError`，不自动直穿障碍。返回航点、几何长度、规划耗时和检查边数，供后续路线摘要使用。
3. `controller.py`：双积分器的解析目标速度控制；用二阶 CBF 不等式与速度/加速度约束求小型 QP，将三轴名义加速度投影到局部可行集合。检查整段零阶保持轨迹的膨胀地图净空；不可行时给出显式失败诊断和紧急制动动作，**不宣称不可行状态仍有安全保证**。
4. `simulation.py`：以相同任务持续飞行，记录完整位置/速度/加速度轨迹。发生接触时原位修复并将三个速度分量清零，任务继续；统一碰撞计数每步最多一次，安全 cost 为 0/1，首次或前一步干净后的每类原始罚项为 −1.2，连续接触为固定 −0.42。

当前默认动力学与旧环境相同类型（受限三维双积分器），不是经过实机验证的四旋翼动力学。首版不做电量决策、不输出“能量安全保证”；后续能耗模型应使用这里保存的实际速度/加速度轨迹和载荷状态校准。

## 观测与公平性

解析导航接收**显式提供的静态三维地图**，可看见长方体和圆柱的边界；高层调度器只应接收位置、订单、站点以及路线时间/能耗摘要，不应直接取得障碍物真值。部署若没有事先测绘地图，需要另做 LiDAR 建图与动态障碍处理；本版没有把 simulator 隐藏几何自动当作传感器观察。动态障碍暂不支持。所有后续高层方法必须共用同一低层版本。

可见图只枚举有限的侧绕/上越候选，**不是完备规划器**。当直线路径被复杂障碍网络阻断时，即使存在航路也可能返回 `NoRouteError`。局部 CBF 也只在模型与离散执行检查成立时提供局部约束；不能凭一次 0 接触 smoke 声称跨地图无碰撞。规划候选图的最坏边数随候选节点数二次增长，需在多障碍地图记录实际耗时，并控制候选节点上限。

## 已做的针对性核查

执行 `uv run python -m unittest discover -s tests -v`：**8/8 通过**，覆盖有限圆柱的上下绕行、长方体净空、贯穿平面墙的上越飞行、CBF 介入、完全封闭墙显式无路、接触修复与固定重复罚项，以及固定资格清单、检查点恢复与源码摘要拒绝机制。

执行 `uv run python -m nav3d.smoke --output artifacts/nav3d_smoke_20260923_verified`：混合高度盒/圆柱场景，142 个 0.05 秒控制步，模拟飞行 7.10 秒，到达目标；统一接触 0，CBF 初始条件或 QP 不可行步 0，CBF 介入 108 步。轨迹和指标保存在 [smoke 结果](artifacts/nav3d_smoke_20260923_verified/summary.json) 与 `trace.npz`。这只是单条小场景执行，不是导航资格率。

## 后续资格评测怎么决定是否需要重训 SAC

在新项目 pickup–delivery 环境实现之前，先冻结地图生成器和低层接口；按地图 ID 留出同类未见地图。航路对必须包括空载/载货、不同高度、盒/圆柱/街区封堵、近站与远站；真实飞行时记录到达率、统一接触、无路率、导航 timeout、实际时间/能耗，以及航路查询和每控制步 QP 的 P50/P95/P99 延迟。结果按地图统计，失败不能删。SAC 重训只在该解析版的失败类型和成本清楚后作为替代低层进行同条件比较，不能同时更改订单流、能耗模型或评价标准。

资格测试样本与运行规则已冻结在 [资格协议](NAV3D_QUALIFICATION_PROTOCOL.md)：12 张同类新地图，每图 4 条直线受阻、4 条畅通，共 96 条。`artifacts/nav3d_qualification_20260923/` 已完成首个检查点 2/96，两个航段均到达、统一接触 0、CBF 不可行步数 0，清单、逐条结果和轨迹均已保存。这仅证明新执行路径与保存机制正常，不是 96 条的到达率或安全认证。当前按启动约束暂停；需继续时运行单行命令：`uv run python -m nav3d.qualify --output artifacts/nav3d_qualification_20260923 --resume`。
