# 文献机型与 1 km 配送场景：参数重估

日期：2026-09-23。状态：**新配送环境的候选规格，未修改已冻结的 60 m 导航 v2，未运行新实验**。用户希望参考原项目 `4000 × 4000 × 400 m` 的环境，但把水平速度上限定为约 `7 m/s`，并考虑 `1000 × 1000 × 100 m`。本页把“论文使用了某机型”“论文使用了某公开数据”“厂商公布的能力”“本项目拟设参数”分开。

## 其他论文并未统一采用 Matrice 100

| 独立来源 | 实际机型／角色 | 对本课题的可迁移内容与限制 |
|---|---|---|
| [Rodrigues et al., *Scientific Data*, 2021](https://www.nature.com/articles/s41597-021-00930-x)，[公开数据](https://doi.org/10.1184/R1/12683453) | **Matrice 100** 实飞；209 条记录，含 0/0.25/0.5 kg 载荷、4–12 m/s 等组合。 | 最适合做小件配送的公开能耗锚点；它并非领域默认机型。论文加装设备后报告的 3.680–4.180 kg 起飞质量超过普通 M100 官方 3.6 kg 最大起飞质量，必须单列这一冲突。 |
| [Rodrigues et al., *Patterns*, 2022](https://doi.org/10.1016/j.patter.2022.100569)，[Muli et al., GIoTS 2022](https://doras.dcu.ie/27277/1/Main.pdf) | 对前述 **同一个 M100 数据集**进行建模或模型比较。 | 不能把这些文章计成三组独立 M100 实飞证据。 |
| [Gong et al., *IEEE TAES*, 2023](https://arxiv.org/abs/2209.04128) | **Matrice 210 RTK V2** 实飞；测水平 0–15 m/s、升降和功率，建三维稳态模型。 | 支持不同工业四旋翼的速度—功率关系；原文没有验证载货三维频繁加减速全过程。 |
| [Bauersfeld and Scaramuzza, *IEEE RA-L*, 2022](https://arxiv.org/abs/2109.04741) | 实飞是 **752 g 自制四旋翼**；另把 DJI Mavic 2/3、Matrice 200/600 Pro、Parrot Anafi AI、Skydio 2 的厂家规格代入模型比较。 | 强调机体/旋翼/电池参数决定续航和最优速度；六款商用品的表格是**模型估计与厂家规格比较**，不是六款都实飞验证。Matrice 200 也不是 M100。 |

因此：保留 M100 作为**首版可复现的载荷数据基准**，不要宣称它是其他论文的统一标准。可在方法稳定后用 M210/M200 类较大平台作参数迁移或敏感性测试，但不能把 M210 的功率数据直接拼到 M100 机体和电池上。原机型、重量及候选能源参数详见 [上一份调研](PLATFORM_ENERGY_SURVEY_20260923.md)。

## 原项目与候选场景的真实比例

读取 `/home/zjl/mappo/envs/UAVEnergyDeliverySAC.py` 的默认值：原环境为 `4000 × 4000 × 400 m`，水平速度上限 `20 m/s`，竖直速度上限 `5 m/s`，水平／竖直加速度上限 `5/3 m/s²`，策略周期 `0.20 s`，每任务最多 4000 步。默认 LiDAR 最大量程 `100 m`，24 圆柱是原生 SAC 资格测试采用的配置，不是构造器默认（构造器默认为 0 障碍）；该资格测试圆柱贯穿全高。新解析导航 v2 的 `60 × 60 × 24 m` 和 `7/4 m/s`、`4/3 m/s²` 是另一套几何与动力学资格测试，不能把其 96/96 直接移植到 1 km 地图。

下表仅算**最理想的直线、全程顶速下限**，不含启停、升降、避障、取送货或返站：

| 地图与水平速度 | 走完 1 条边 | 对角线单程 | 对角线往返 |
|---|---:|---:|---:|
| 原 4 km，20 m/s | 200 s | 283 s | 566 s |
| 原 4 km，改 7 m/s | 571 s | 808 s | 1616 s |
| 候选 1 km，7 m/s | 143 s | 202 s | 404 s |

`4 km/20 m/s` 与 `1 km/7 m/s` 的单程边界时间分别为 200 和 143 s，处于相近数量级；直接只把旧地图速度改成 7 m/s 会让一次远程服务/返航过长。按 M100 厂家 [TB47D 悬停时间](https://www.dji.com/matrice100)，附加载荷 1 kg（这里可拆成 0.50 kg 设备与 0.50 kg 货物）的标称悬停时间约 13 min；**这不是实测航段预算，也不等于 7 m/s 续航时间**，只能说明 808 s 单程加返航不适合当作典型订单。1 km 对角线往返约 404 s 下限，能让充电选择和长短订单同时有讨论空间；实际可行性仍必须按所选功率模型和保守误差核查。

按原 `0.20 s` 策略周期，4 km 地图若降到 7 m/s，理想对角线单程也要约 **4041 步**，已略超原来的每任务 4000 步上限；这尚未包含任何避障。因此“只改速度、不改地图”会把正常远程航段误判为导航超时。

## 候选规格与需要重新验证的项目

- 配送区域暂设 `1000 × 1000 × 100 m`，连续物理时间不缩放；高层候选订单为取货点→送达点，并持续随机到达。水平速度上限暂取 `7 m/s`，竖直速度和加速度先以现有 v2 的 `4 m/s` 与 `4/3 m/s²` 为候选，再独立核查满载机动能力；将 7 m/s 记为**控制上限**而不是恒定巡航速度。它处在 M100 实飞的 4–12 m/s 速度组合内，但这些实验是较长开阔航线。[原始数据](https://doi.org/10.1184/R1/12683453)
- M100＋TB47D、0/0.25/0.50 kg 货物、约 0.50 kg 机载设备可作首版质量族；满载总质量约 3.355 kg，低于 3.6 kg 厂家最大起飞质量。设备质量和其电耗为可追溯**候选假设**，不能说已实机验证。[DJI 规格](https://www.dji.com/matrice100)
- 原环境的空间相关参数不可原样照搬：`task_z_min/max=20/380 m` 在 100 m 高地图非法，`xy_sampling_margin=100 m`、`minimum_task_distance=100 m`、圆柱半径 `50–120 m` 会显著改变可用区域和障碍密度。首版同类图可先按原地图的 1/4 几何比例**提出候选**：高度 5–95 m、XY 边缘 25 m、最短任务 25 m、圆柱半径 12.5–30 m；随后按飞行器尺寸、目标容差及有效航路重新检查，不将比例值自动视为最终值。若仍使用 24 根圆柱，须报告实际可通行率／绕障倍率。
- **不缩放实体机尺寸或传感器物理量程**：原 `safe_radius=0.5 m` 不应变 0.125 m；原目标半径 5 m 是否保留，应以定位误差和配送点服务定义决定；原 LiDAR 100 m 也不能因为地图缩为 1/4 就自动变 25 m。若改用某款实际 LiDAR，再核对其不同反射率下的量程与现有 1024 射线观测是否匹配。
- 旧环境的 `4000` 步任务上限等于 `800 s`。在 1 km 图它可能掩盖陷阱航路，任务时限、订单 deadline、到达率和能源预算要按**新场景**重新设定并报告；不能把 800 s 误叫现实外卖 SLA。
- 当前 60 m v2 的导航、CBF/QP 和路线规划可以当实现起点，但 **1 km 地图是新尺度**。先另建配置与资格集，再看新地图到达率、统一接触、QP 不可行、运行时间及能量预测误差；保持冻结 60 m 结果独立。大街区隔断仍作为之后的结构变化套件。

本页明确研究场景与文献依据。用户批准后，候选机型、控制上限和 24 圆柱参考地图已落地于 [`delivery_1km/scenario.py`](delivery_1km/scenario.py)。按用户随后指出的三维障碍要求，**新地图圆柱高度为 15–75 m、低中高各 8 根，不再贯穿 100 m 高度**；这与上述原生 SAC 旧地图不同。后续确定每图一座位置随机的站、分散随机取送点、地面和可达屋顶服务位；实现见 [`delivery_1km/README.md`](delivery_1km/README.md)。部分充电使用明确标注的 100 W 上限曲线假设并扫描敏感性，不能称作 M100 实测。模块还不是完整配送环境。订单生成率、deadline、可用 SOC 和物理能耗系数尚需按 1 km 航段及机型校准，不能由几何缩放直接决定。没有更改冻结导航源码。
