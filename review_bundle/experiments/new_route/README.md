# Superseded E1

`e1_energy.py` is retained as executable provenance for the invalid 2026-08-13 E1 artifacts. Its one-task-action-then-charger collection protocol is not part of the active staged energy-transfer route.

Do not mix its partial `raw_sorties.jsonl` with Stage A data. Active bootstrap code lives in `experiments/energy_transfer/`.
