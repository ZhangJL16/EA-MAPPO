> **LEGACY / SUPERSEDED:** Historical research provenance only. This file is not an active method, theorem, implementation, test, or README authority. The active route is in docs/new_theory/.

# Assumption Ledger

| ID | Assumption | Class | Why needed | Falsification / what breaks | Weakening status |
| --- | --- | --- | --- | --- | --- |
| AS-PHYS | `Post_hat`, `Tube_hat`, and `EnergyTube_hat` soundly cover the endpoint, complete swept motion, and lower-energy prefix on every pre-published covered interval | REQUIRES_CALIBRATION | Converts finite certificate predicates into a physical guarantee | Endpoint membership can hide obstacle crossing or intra-step depletion | Open; can localize by certified cell/domain |
| AS-ENERGY | Each finite nonnegative `d_bar_nu` upper-bounds realized flight consumption at recovery node `nu` | REQUIRES_CALIBRATION / SYNTHETICALLY_VALIDATED | Supports energy recursion and reserve preservation | Underestimation can exhaust energy before terminal | Three 2× synthetic atlases pass full-domain analytic domination; physical calibration remains open |
| AS-GEOM | Verified FREE excludes true obstacles and unknown space for the complete swept tube | REQUIRES_CALIBRATION | Collision preservation | False FREE label invalidates collision theorem | Static-obstacle scope is strong and explicit |
| AS-KAPPA | Frozen `kappa` certificates are bound to live versions, dependency fingerprints, validity time, and a positive selected rank; they cover the current tube and map every robust successor to an exact hash-declared committed lower-rank child. Positive children remain BACKUP under terminal overlap; only the exact level-zero child may enter CHARGING | IMPLEMENTED_AND_TESTED / REQUIRES_CALIBRATION | Finiteness, nonempty fallback, safe recovery | Dependency replacement, expiry, rank-zero source, missing child coverage, or overlap-based child substitution breaks authority and induction | Runtime snapshots active child id/rank/hash, verifies realized membership, and transactionally commits KAPPA-ARRIVE; physical coverage remains a calibration premise |
| AS-PROPER | Recovery descends one level per step, or within bounded `M` steps | MATHEMATICAL / SYNTHETICALLY_VALIDATED | Existence and finite `E^kappa` without discount | Same-level cycles with positive cost make energy unbounded | Bounded-M route not implemented |
| AS-GEN | `G` is square full-rank with certified lower singular-value bound and full-zonotope inclusion | IMPLEMENTED_AND_TESTED / REQUIRES_CALIBRATION | Lebesgue density and positive-volume task support | Rank loss invalidates determinant density | Applies only to primary 3-D affine-tanh candidate |
| AS-FROZEN | Certificate correspondence and model are frozen within each RL proof/update batch | IMPLEMENTED_AND_TESTED | Fixed Bellman operator and epoch consistency | Changing support invalidates fixed-operator comparison | Replay epoch now composes runtime snapshot epoch and mission manifest |
| AS-WCET | The Level-0 controller is single-threaded and non-reentrant for `step/reset`; snapshot reads are pure, asynchronous in-scope certificate writers share the lock, compare-and-publication establishes command coverage, and postinterval classifier/child readback is bound before hybrid-state publication | IMPLEMENTED_AND_TESTED: PARTIAL / REQUIRES_DEPLOYMENT_EVIDENCE | Prevents stale commands and incompatible successor modes | Reentrant control calls, an unregistered writer, late overwrite, expiry, or child drift falls outside Level 0 or ends coverage | Software detects injected command, arrival, timestamp, and child drift; Python timing is not WCET and hardware/bus atomicity remains external |
| AS-CHARGE | Hybrid charging executes the certified hold/support, preserves collision and nonnegative energy throughout the interval, deducts flight cost, adds bounded charge, and has successor in `G_ch` | IMPLEMENTED_AND_TESTED: PARTIAL / REQUIRES_CALIBRATION | Charging invariance | Endpoint charge can conceal prefix depletion | Hold execution/logging repaired; calibrated complete hybrid envelope remains open |
| AS-ARRIVE | A voluntary arrival is a covered `RUN` successor with a live successor-bound station certificate; its mode update has zero duration and receives no charging gain on that flight interval | IMPLEMENTED_AND_TESTED / REQUIRES_CALIBRATION | Closes `NORMAL -> CHARGING` without misclassifying the physical action | Reclassifying an unverified terminal overlap or charging on the same interval breaks the hybrid case split | Runtime checks the successor station-hold predicate and delays charging to a later charging-source interval |
| AS-GOAL | Goal affects actor only and is excluded from strict support construction | IMPLEMENTED_AND_TESTED / SYNTHETICALLY_VALIDATED | Arbitrary admissible goal sequence recoverability | Goal leakage changes certificate at fixed physical state | Preserve; task completion is not implied |
| AS-TEACHER | Teacher and learner transitions share the same MDP transition/reward semantics and clean observation contract | IMPLEMENTED_AND_TESTED: PARTIAL | Off-policy replay validity | Privileged online features or altered reward invalidate equivalence | Benefit/convergence remain empirical |
| AS-RESERVE | Reserve is constant on an executable proof edge, or E3 includes successor reserve increase | MATHEMATICAL | Preserves energy feasibility | A larger successor reserve invalidates the old induction | Executable profile should use a constant reserve |
| AS-SENSOR-SOUND | A certificate-eligible return is nearest-obstacle complete over the contracted beam; unmodeled false negatives revert cells to UNKNOWN | REQUIRES_CALIBRATION | Justifies FREE promotion | Range/pose bounds alone do not exclude a missed nearer obstacle | Newly explicit; no aircraft evidence |

Classification is deliberately split when software semantics are tested but physical premises remain uncalibrated.
