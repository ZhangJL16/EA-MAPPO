# Theorem Ledger

Status labels: `DRAFT`, `PROVISIONAL`, `UNJUSTIFIED`, `REFUTED`, `VERIFIED-CONDITIONAL`. The row labels record the last accepted mathematical disposition. Bundle-level proof-audit provenance and the exact accepted input hashes are recorded separately in `PROOF_AUDIT.md`/`.json`; any later byte change invalidates that hash-specific verdict.

| ID | Statement | Dependencies | Current status | Current issue / next proof obligation |
| --- | --- | --- | --- | --- |
| TH-E1 | Frozen-`kappa` exact joint robust first-passage energy on paths covered through arrival is finite; the node recursion `d_bar_nu+max E_mu` gives a stored upper | nonnegative selected DAG; exhaustive selected-child linkage; bounded nonnegative cost; complete recovery coverage | VERIFIED-CONDITIONAL | No physical first-passage bound is claimed after coverage loss; physical cost calibration remains external |
| TH-R1 | `C in A_rec_set(x)` implies every robust successor of the directly verified complete support lies in `R` | sound complete-set `Post_hat_C`; current proof-object validity | VERIFIED-CONDITIONAL | Definition-level full-support predecessor result; no singleton inheritance is used |
| TH-R2 | Repeated verified learned actions preserve recoverability over complete covered intervals | TH-R1; pre-publication full-interval coverage | VERIFIED-CONDITIONAL | No latent physical invalidity is assumed observable as a stopping time; physical publication remains external |
| TH-V1 | Ideal energy-augmented finite-atlas `R_RL` exists as greatest fixed point and is a subset of `R` | finite cells; monotone full-support `Phi`; candidate subset of `R` | VERIFIED-CONDITIONAL | Finite lattice proof is elementary; current code is only a graph-kernel approximation |
| TH-C1 | Closed-gate charging is forward invariant | complete charge/hold actuator, swept-tube, energy-prefix and hybrid-successor certificate | VERIFIED-CONDITIONAL | Endpoint charge alone is insufficient; physical charging-envelope calibration remains external |
| TH-D1 | Certified departure returns to `R` with the same pending task | lower-energy scalar gate; complete successor inclusion; atomic mode reset; explicit task rule | VERIFIED-CONDITIONAL | Runtime scalar gate now uses certificate-state `e^-`, not nominal battery; return to optional `R_RL` is not claimed; physical outer-set soundness remains external |
| TH-L1 | Covered flight, optional successor-certified `RUN-ARRIVE`, recovery, charging, departure, and same-task resume are safety-closed over complete covered intervals | primitive verification schema; selected-child descent; energy-prefix bounds; exhaustive source/rank transition relation; zero-duration arrival predicate | PROVISIONAL | `RUN-ARRIVE` and the charging/rank-zero source restrictions repair reachable counterexamples; fresh current-hash proof audit is required before restoration to `VERIFIED-CONDITIONAL` |
| TH-G1 | Affine-tanh map is a diffeomorphism onto the zonotope interior with the stated change-of-variables density | square invertible `G`; finite Gaussian parameters | VERIFIED-CONDITIONAL | Algebra closes under the explicit hypotheses; temperature semantics still require the physical Jacobian term |
| TH-G2 | Hybrid Generator/`kappa`/charger/fail-closed Bellman branches match publication-time execution authority and the actual publication support/epoch | recorded authority; actual publication `c,G,u`; final `covered_at_publication`; positive-rank/source eligibility for κ; no density assigned to atoms; fail-closed zero bootstrap | PROVISIONAL | New regressions bind replay to the actual publication context, refresh successor observations, reject stale pre-step dependencies/expiry, and forbid κ from charging or rank zero; complete-suite rerun pending |
| TH-F1 | Strict safety is independent of learned collision/energy fields | fields rank only candidates already mapped through the same verified `c,G`; certificate modules cannot import field code; non-Generator authority bypasses ranking | VERIFIED-CONDITIONAL | Import-boundary, containment, and fallback tests close the software contract; learned contribution and physical safety remain separate questions |
| TH-H1 | A one-dimensional bottleneck with strictly monotone readouts cannot preserve two nonidentical/nonreversed proposal rankings; finite-DAG recovery residual accumulates at most linearly in remaining rank | Proposition H0; Proposition H1; finite selected DAG | VERIFIED-CONDITIONAL | These results justify distinct outputs and calibration budgets, not separate encoders/networks or empirical benefit; matched ablation remains open |
| TH-T1 | Each clean teacher tuple is a real same-MDP transition instance; successful-only retention is outcome-conditioned initialization rather than generally unbiased Bellman data | identical environment transition/reward semantics; clean observation contract; explicit selection rule | VERIFIED-CONDITIONAL | No convergence, benefit, unbiased stochastic-kernel, or charging-competence claim follows; 2x2 causal factorial remains open |
| OLD-L1 | Clipped nonnegative energy envelope contains every plant successor | plant allows negative energy | REFUTED | Remove clipping or require pre-step lower energy at least worst-case cost |
| OLD-T1/T7 | Collision/energy invariance for all time under the old D10/E3 | current swept tube omitted; reserve varies; certificates expire | REFUTED | Replace by stopped-time theorem with corrected action and reserve predicates |
| OLD-T-AUTH3 | Current atlas graph pruning proves lifecycle completion | graph kernel omits energy/full supports/terminal seed and no liveness premise | REFUTED | Claim only candidate topology until state-level verifier exists |

The pre-repair narrow theorem failed a fresh blind audit with one FATAL and eight CRITICAL findings. `PAPER_THEOREM.md` contains the corresponding repairs, and the Round-5 normalized blind review accepted the affected rows as `VERIFIED-CONDITIONAL` with zero open issues. Round 6 then repaired a runtime publication-authority violation and synchronized the theorem bundle; subsequent hash-specific audit history is maintained in `PROOF_AUDIT.md`. Any acceptance remains same-family/provisional, not a hardware guarantee; physical calibration and complete-interval publication remain external premises.
