# 1 km 导航资格 v1 修复协议

日期：2026-09-23；在 v1 结果读取前固定。原 v0 源码和 33 条原始结果保存在 `artifacts/delivery_1km_nav_20260923/`。v0 的 33 条已有 6 条 `candidate graph exceeds 650 nodes`，因此按原协议要求的至少 92/96 成功已无法通过；v0 在第 34 条服务点生成时停止。不能把 v0 的局部结果与 v1 合并。

v1 仅替换 1 km 地图的路线规划器：直线安全则直飞；被障碍物阻挡时，构造高于最高障碍物的上升—平飞—下降航段，并以完整充气地图逐段校验。该备选路线仅保证几何可行，不声称最短路或最低能耗。冻结 v2 轨迹跟踪器、CBF/QP、碰撞修复、物理步长、速度和加速度限制不变。新版本在 manifest 里事先保存全部 96 条的起点、终点和每图充电站，避免按执行进度重新抽样。服务点仍由地图几何独立生成，不读取任何策略表现；地面点和屋顶点各自可达充电站。

地图 100–111、四类航段各 2 条/图、800 模拟秒上限、5 m 到达半径及记录字段沿用 [v0 协议](DELIVERY_1KM_NAVIGATION_PROTOCOL_20260923.md)。**通过线仍为至少 92/96 到达，且总统一接触数为 0**；单列四种航段失败与 QP 不可行步。v1 端点抽样与 v0 不完全相同，因此它是一个新的预定资格轮次，不把两轮到达率作配对增益结论。额外固定回放 v0 六个失败端点，仅用于确认 650 节点问题已消除。

本轮只评价导航执行，不产生真实飞行 Wh、载荷能耗、动态订单、部分充电或学习效果结论。若 v1 通过，下一步仍须独立建立能耗模型和订单环境。无需神经训练。

一行运行命令：`cd /home/zjl/uav_learning_research && .venv/bin/python -m delivery_1km.qualify --output artifacts/delivery_1km_nav_v1_20260923`
